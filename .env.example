# Required. The app reads conferences and speakers from this database.
# Without it the app falls back to local SQLite and will simply be empty.
DATABASE_URL=

# Optional. Local SQLite path used when DATABASE_URL is not set.
DATABASE_PATH=

# Optional. Rendered fallback for blocked or JavaScript-only agenda pages.
FIRECRAWL_API_KEY=
