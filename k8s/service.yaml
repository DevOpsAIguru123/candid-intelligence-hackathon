apiVersion: v1
kind: Service
metadata:
  name: speaker-signal-service
  labels:
    app: speaker-signal-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: speaker-signal-app
